#!/bin/bash
set -e

# Reads a single line "root:<newpassword>" from stdin and applies it
# non-interactively, since `passwd` requires a real controlling tty
# and can't be driven from a piped subprocess.
read -r line
printf '%s\n' "$line" | sudo -A chpasswd
